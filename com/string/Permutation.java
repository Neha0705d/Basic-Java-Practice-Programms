package com.string;

import java.util.Scanner;

public class Permutation {

	
	/*
	 * private static String permutation(String perm, String word) { if
	 * (word.isEmpty()) { System.err.println(perm + word);
	 * 
	 * } else { for (int i = 0; i & lt ; word.length(); i++) { permutation(perm +
	 * word.charAt(i), word.substring(0, i) + word.substring(i + 1, word.length()));
	 * } }
	 * 
	 * }
	 * 
	 * public static void main(String[] args) {
	 * 
	 * Permutation pm = new Permutation(); Scanner sc = new Scanner(System.in);
	 * System.out.println(" Enter the string : "); String input = sc.nextLine();
	 * String result = pm.permutation(input, input); System.out.println(result); }
	 */
}
